import Options from "../models/options.js";
import Conversation from "./conversation.js";
declare class ChatBot {
    token: string;
    options: Options;
    conversations: Conversation[];
    constructor(token: string, options?: Options);
    addConversation(conversationId: string): Promise<Conversation>;
    getConversation(conversationId: string): Promise<Conversation>;
    ask(prompt: string, conversationId?: string): Promise<string>;
    askStream(data: (arg0: string) => void, prompt: string, conversationId?: string): Promise<string>;
}
export default ChatBot;
